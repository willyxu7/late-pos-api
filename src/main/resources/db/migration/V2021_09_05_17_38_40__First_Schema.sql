CREATE TABLE categories (
    id varchar(36),
    primary key (id)
);

CREATE TABLE products (
    id varchar(36),
    primary key (id)
);

CREATE TABLE variants (
    id varchar(36),
    primary key (id)
);
